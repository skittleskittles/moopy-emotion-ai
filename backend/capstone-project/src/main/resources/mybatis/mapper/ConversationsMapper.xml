<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="dev.capstonebackend.capstone_project.dao.ConversationsDao">

    <!-- 插入记录 -->
    <insert id="insertNewConversation" parameterType="dev.capstonebackend.capstone_project.domain.Conversation"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO conversations (user_id)
        VALUES (#{userId})
    </insert>

    <select id="selectByUserId" resultType="dev.capstonebackend.capstone_project.domain.Conversation">
        SELECT *
        FROM conversations
        WHERE user_id = #{user_id}
    </select>



</mapper>
